enum Gender { Male, Female, Other }

enum TripMode { PICK_UP, DROP_OFF }

enum RouteFilter { Morning, Evening, Faculty, Hostel, Testing }

enum RouteType { IN_LINE, IN_LOOP }

enum LoginMode { Passenger, Driver }

